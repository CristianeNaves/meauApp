import styled from 'styled-components/native';

const LeftedContainer = styled.View`
  margin-left: 60px;
`;

const Container = styled.ScrollView`
  margin-right: 24px;
  margin-left: 24px;
`;

export {LeftedContainer, Container};
